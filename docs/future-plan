
1、丰富查询选择条件

支持增加嵌套字段查询，wildcard通配符查询、matchPhrase匹配短语查询，scriptQuery脚本查询等。

2、增加聚合功能模块

支持聚合查询，桶聚合，指标聚合。

3、丰富api示例
覆盖绝大部分的api。提供精确的示例帮助更好的使用api。

4、增加ES技术文章模块
提供交流学习的模块，可以查询公共共享文章，或者提交自己的文章到公共共享，让别人看到，交流学习。
